{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfg.json", "src"],
      "outputs": ["dist/**", "build/**"]
    },
    "lint": {
      "dependsOn": ["eslint-config#build", "^lint"],
      "inputs": ["lib", "src", "eslint.config.js"]
    },

    "check-types": {
      "dependsOn": ["^check-types"],
      "inputs": ["tsconfg.json", "src"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfg.json", "src"],
      "outputs": ["dist/**", "build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
