{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfg.json", "src"],
      "outputs": ["dist/**", "build/**"]
    },
    "lint": {
      "inputs": ["lib", "src", "eslint.config.js"],
      "dependsOn": ["^lint"]
    },

    "check-types": {
      "inputs": ["tsconfg.json", "src"],
      "dependsOn": ["^check-types"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfg.json", "src"],
      "outputs": ["dist/**", "build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
